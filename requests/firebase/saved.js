import firebase from './config'
const db = firebase.firestore()

async function saved_movie(movie, firebaseUser, currently_liked) {

    // uid,
    // liked,
    // disliked,
    // tags

}

export default saved_movie